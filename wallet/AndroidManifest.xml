<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.buli.w"
    android:installLocation="internalOnly"
    android:versionCode="102600"
    android:versionName="10.26">

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />

    <!-- permissions ... (giữ nguyên như bạn gửi) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:name="net.buli.w.WalletApplication"
        android:hasFragileUserData="true"
        android:allowBackup="false"
        android:icon="@drawable/ic_app_color"
        android:label="@string/app_name"
        android:localeConfig="@xml/locale_config"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name="net.buli.w.ui.WalletActivity"
            android:configChanges="keyboard|keyboardHidden"
            android:exported="true"
            android:label="@string/app_name_short"
            android:launchMode="singleTask"
            android:theme="@style/My.Theme.WithSplash" />

        <!-- FIX: thêm đúng chỗ này -->
        <activity android:name="net.buli.w.ui.NotificationsActivity" android:exported="false" />

        <activity-alias
            android:name="net.buli.w.WalletActivity"
            android:exported="true"
            android:targetActivity="net.buli.w.ui.WalletActivity">
            <intent-filter android:label="@string/app_name_short">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <data android:mimeType="application/x-btctx" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity-alias>

        <!-- các activity khác giữ nguyên như file gốc của bạn -->
        <activity android:name="net.buli.w.ui.send.SendCoinsActivity" ... />
        <!-- ... copy tiếp toàn bộ phần còn lại từ file gốc ... -->
    </application>
</manifest>
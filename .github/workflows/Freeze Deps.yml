name: Build APK
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -f build.Containerfile --output out .
      - uses: actions/upload-artifact@v4
        with:
          name: bitcoin-wallet-apk
          path: out/*.apk